# arbitrary-precision-arithmetic for big number calculate
Implement arbitrary-precision arithmetic by representing numbers as digit strings for big number calculate.

Written using basic C language syntax, without relying on any libraries, such that both C and C++ compilers can compile it correctly.

Read .h file for more information